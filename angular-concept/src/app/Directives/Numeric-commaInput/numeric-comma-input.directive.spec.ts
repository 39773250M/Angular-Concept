import { NumericCommaInputDirective } from './numeric-comma-input.directive';

describe('NumericCommaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericCommaInputDirective();
    expect(directive).toBeTruthy();
  });
});
