import { RegexHelpersPipe } from './regex-helpers.pipe';

describe('RegexHelpersPipe', () => {
  it('create an instance', () => {
    const pipe = new RegexHelpersPipe();
    expect(pipe).toBeTruthy();
  });
});
